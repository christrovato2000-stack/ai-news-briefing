# Core HTTP & parsing
requests>=2.31.0
feedparser>=6.0.11
beautifulsoup4>=4.12.0
lxml>=5.2.0

# Anthropic Claude API
anthropic>=0.34.0

# HTML email templating
Jinja2>=3.1.4

# Useful extras (already in stdlib on 3.11+ but pin for older envs)
# smtplib is stdlib — no install needed
